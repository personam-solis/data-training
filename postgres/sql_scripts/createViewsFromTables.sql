-- Media by year (media_year)
CREATE VIEW IF NOT EXISTS public.media_year AS
    SELECT 
;

-- All Actors in media (actors_media)
CREATE VIEW IF NOT EXISTS public.actors_media AS
    SELECT 
;

-- All Crew in media (crew_media)
CREATE VIEW IF NOT EXISTS public.crew_media AS
    SELECT 
;

-- All media by genre (media_genre)
CREATE VIEW IF NOT EXISTS public.media_genre AS
    SELECT 
;
